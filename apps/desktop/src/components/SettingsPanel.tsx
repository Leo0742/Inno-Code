export function SettingsPanel() {
  return (
    <section className="panel">
      <h3>Settings</h3>
      <p>Provider profiles (MVP): OpenAI-compatible endpoints, role model mapping, approvals.</p>
    </section>
  );
}
